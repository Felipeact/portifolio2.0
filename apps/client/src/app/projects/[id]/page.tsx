import { Outfit, Roboto } from 'next/font/google'

// const outfit = Outfit({
//   subsets: ['latin'],
//   display: 'swap',
// })

// const roboto = Roboto({
//   subsets: ['latin'],
//   display: 'swap',
//   weight: '400'
// })


export default function ProjectsId({ params: { id }}: { params: { id: string }; }) {

  return (
    <main className="min-h-screen ">
      <p>Working ...</p>
    
    </main>
  )
}
